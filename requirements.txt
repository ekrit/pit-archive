yfinance>=1.5.0
vaderSentiment>=3.3.2
requests>=2.31.0
pandas>=2.0.0
numpy>=1.24.0
# Optional: enables the gradient-boosting model in scraper/model.py.
# The pipeline falls back to a pure-numpy logistic regression without it.
scikit-learn>=1.3.0
